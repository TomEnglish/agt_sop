// Copy to config.js and fill in. The anon key is public by design — it is shipped to the
// browser and grants nothing but execute on the six sop_* functions. Never put the service
// role key here.
window.SOP_CONFIG = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-KEY"
};
